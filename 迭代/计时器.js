/* let intervai = setInterval(() => {
      if (this.writeUserId.length > 0) {
        this.getEventDataFromService()
        clearInterval(intervai)
      }
    }, 50) */
    /*  setTimeout( ()=> {

      alert(this.userId)
    },500) */